import 'package:flutter/material.dart';

class CarouselLoadErrorWidget extends StatelessWidget {
  const CarouselLoadErrorWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}